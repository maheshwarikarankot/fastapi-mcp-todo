services:
  - type: web
    name: fastapi-mcp-todo
    runtime: python
    plan: free
    autoDeploy: true
    buildCommand: pip install -r requierments.txt
    startCommand: uvicorn fastapi-mcp-todo.main:app --host 0.0.0.0 --port $PORT

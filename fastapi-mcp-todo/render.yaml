services:
  - type: web
    name: fastapi-mcp-todo
    runtime: python
    plan: free
    autoDeploy: true
    buildCommand: pip install -r requierments.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
